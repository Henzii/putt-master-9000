import React from 'react';
import { FC } from "react";
import { View, Text} from "react-native";

const NewDistance: FC = () => {
    return (
        <View style={{ flex: 1, padding: 16 }}>
            <Text>New Distance</Text>
        </View>
    );
};

export default NewDistance;