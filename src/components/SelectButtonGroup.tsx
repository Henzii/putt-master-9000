import React, { PropsWithChildren } from 'react'
import { View, Text } from 'react-native'

export default function SelectButtonGroup({ children } : {children: JSX.Element}) {
    return (
        <>
        { children }
        </>
    )
}
